# 占位文件：保持 content/ 目录存在于 git。
# CI 的 scripts/generate-content-manifest.mjs 在 content 目录缺失时会 process.exit(1)
# 导致 deploy-workers.yml 的 "Generate content manifest" step 失败。
# 此占位让 content 目录存在（空），脚本扫描得 0 个 locale 子目录，生成空 manifest {} 并正常退出。
# 后续内容 part 填充真实 mdx 后，此文件可保留或删除。
